import { CursorHoverDirective } from './cursor-hover.directive';

describe('CursorHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorHoverDirective();
    expect(directive).toBeTruthy();
  });
});
